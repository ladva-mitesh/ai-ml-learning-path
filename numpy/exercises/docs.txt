-> import numpy as np
-> __version__
-> np.arange
-> np.full
-> np.reshape
-> array.copy()
-> np.vstack
-> np.hstack
-> np.repeat() : repeats each element
-> np.tile() : repeats the whole array
-> np.r[a,b] : Think of it as a shortcut for joining arrays. np.r_[arr1, arr2] is same as np.concatenate((arr1, arr2))
-> intersect1d(a,b) : intersect = common part, 1d = one-dimensional arrays. Gives me the common elements between array a and array b.
-> np.union1d(a,b) : All unique elements from both arrays
-> np.setdiff1d(a,b) : Elements in a but not in b
-> np.setxor1d(a,b) : Elements in either array but not both
-> np.where (condition) : returns a tuple
-> map with Multiple Iterables
-> zip in python
-> To swap any two columns: arr[:, [new_column_order]]
-> np.random.uniform